import REACT from 'react';

const RollDice = (props) => {
    //console.log(props)
    return( 
        <>
        {/* console.log(Math.random() * 6) */}
        {props.number}
        </>
    )
    }

export default RollDice