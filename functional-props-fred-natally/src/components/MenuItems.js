import REACT from 'react'

const MenuItems = (props, thing) => {
    console.log("props: ", props)
    return(
        <>
        <h3>(props.menuItems.name)</h3>
        </>
    )
}

export default MenuItems